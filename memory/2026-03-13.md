# 2026-03-13 — Bolt Work Log

## Kanban Board — Drag & Drop + Bulk Move (Jonny Request)
- **Commit:** `d959ccb` on `yarn-digital/yarn-digital-kanban`
- **What:** Updated kanban board with proper drag-and-drop between columns + multi-select bulk move
- **Details:**
  - Each column is now a proper `useDroppable` target (visual highlight on hover)
  - Checkbox on every task card for multi-select
  - "Select all" button on each column header
  - Floating action bar appears when tasks selected — "Move to..." opens column picker
  - Bulk status updates via PUT /api/tasks (parallel requests)
  - `onPointerDown` stopPropagation on buttons so clicks don't trigger drag
- **Build:** Passes clean, pushed to main, Vercel auto-deploying

## Dashboard — Live Agent Status on Agents Page
- **Commit:** `a96b18c` on `yarn-digital/yd-dashboard`
- **What:** Agents page now shows real working/idle status from kanban board
- **Details:**
  - Fetches `/api/agents/status-live` every 60s
  - Overlays live kanban status on Firestore agent cards
  - Shows "Working" with green badge + current task card when agent has in-progress tasks
  - Shows "Idle" with amber badge when no in-progress tasks
  - Live task counts in stats footer
  - Fixed pre-existing TypeScript error in daily-summary route

## Auto-Publish Documents Pipeline (Jonny Request)
- **Commit:** `025e147` on `yarn-digital/yd-dashboard`
- **What:** Public API endpoint + batch script for automatic document publishing to Firestore
- **Endpoint:** `POST /api/documents/upload` — public, no auth required
- **Batch script:** `/root/.openclaw/workspace/scripts/auto-publish-docs.sh`
- **SOP:** `/root/.openclaw/workspace/sops/auto-publish-documents.md`
- Agents POST to the API when they create deliverables → docs appear immediately on dashboard
- Upserts by title+agent (no duplicates)
- 19 deliverables synced, 45 total docs in Firestore

## Blockers Found
- Kanban board POST /api/tasks is broken for batch writes — Firestore rejects `undefined` in `createdAt` field
- Individual task PATCH `/api/tasks/:id` returns 404 on kanban board (route doesn't exist there)
- Jarvis's PATCH fix was for yd-dashboard, not the kanban board itself
- Agent API endpoints on yd-dashboard return Unauthorized despite middleware public route config — needs investigation
