
## New Skills from Garry Tan's gstack (via Jarvis)
- `eng-plan-review` — architecture/plan review before implementation (4 sections: Architecture, Code Quality, Tests, Performance)
- `pre-landing-review` — pre-merge safety gate (SQL injection, race conditions, XSS, LLM trust boundaries)
- `ship-workflow` — automated release: say "ship" → tests, review, changelog, PR created
- `webapp-testing` (upgraded) — 4 modes: diff-aware, full, quick, regression. Health scoring, per-page checklists
- All symlinked into bolt/skills/

## Kanban API PATCH Fix
- The HEARTBEAT.md had wrong API format: was using `/api/tasks/<id>` (404)
- Correct format: `PATCH /api/tasks` with `{"taskId": "<id>", "updates": {...}}` in body
- Also works via `PUT /api/tasks` with `{"taskId": "<id>", "status": "..."}` (flatter body)
- Fixed HEARTBEAT.md with correct format
- Both PATCH and PUT tested and confirmed working on live deployment

## Landing Page Project — Assigned by Jarvis
- Build high-converting landing page for Meta ad traffic
- Must be mobile-first (Meta traffic = mostly mobile)
- Lead capture form that auto-feeds into YD dashboard (leads API already exists: /api/leads)
- Forms infrastructure also exists: /api/forms
- Pipeline: Scout (copy brief) → Aria (copy) → Bolt (build + wire leads) → Blaze (point ads)
- Deploy as part of yd-dashboard repo
- Waiting on Scout's copy brief and Aria's final copy before building

## Landing Page Deployed — /free-audit
- Live at: https://yd-dashboard.vercel.app/free-audit
- PR #1 merged to master, Vercel production deployment confirmed (sha: bda19769)
- Public lead API at /api/public/leads (rate-limited, zod validation)
- Leads auto-feed into Firestore leads collection
- Meta Pixel ready via NEXT_PUBLIC_META_PIXEL_ID env var (needs setting on Vercel)
- UTM parameters captured for ad attribution
- All page sections verified rendering: hero, social proof, problem, solution, case studies, about, form
- Still needed: NEXT_PUBLIC_META_PIXEL_ID, DEFAULT_ORG_USER_ID, DEFAULT_ORG_ID env vars on Vercel
