Read AGENT-OS-SPEC.md for the full spec. Then build Phase 1 — Agent Team Registry and Task Manager for the yd-dashboard.

CRITICAL RULES:
1. Follow existing patterns EXACTLY — look at src/app/leads/page.tsx and src/app/api/leads/route.ts for the page and API patterns
2. ALL styles are INLINE React CSSProperties — NO Tailwind, NO CSS modules
3. Use existing api-middleware.ts (withAuth, successResponse, handleApiError, validateBody, requireDb, AuthUser)
4. Use existing types pattern in src/types/database.ts — add Agent and Task interfaces there
5. Add AGENTS, TASKS collections to the COLLECTIONS const in src/types/database.ts
6. Use lucide-react icons (Bot, CheckSquare for sidebar)
7. Accent color is #FF3300
8. Use the useAuth() hook and useIsMobile() hook patterns from existing pages

BUILD THESE FILES:
1. src/types/database.ts — Add Agent and Task interfaces + AGENTS/TASKS to COLLECTIONS
2. src/app/api/agents/route.ts — CRUD for agents (follow leads API pattern exactly)
3. src/app/api/agents/[id]/route.ts — GET/PUT/DELETE single agent
4. src/app/api/tasks/route.ts — CRUD for tasks with filters
5. src/app/api/tasks/[id]/route.ts — GET/PUT/DELETE single task
6. src/app/agents/page.tsx — Grid view of agent cards with create/edit modal
7. src/app/tasks/page.tsx — Task manager with kanban board view, list view, recurring tab, filters, create/edit modal
8. src/components/Sidebar.tsx — Add Agents and Tasks nav items after Dashboard before Leads

Make the kanban drag-and-drop work with onDragStart/onDragOver/onDrop and dataTransfer.
After building all files, run: npm run build
